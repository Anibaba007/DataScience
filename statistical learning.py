# -*- coding: utf-8 -*-
"""
Spyder Editor

This is a temporary script file.
"""
import math
print(math.sin(math.pi/2)) #name of the module (e.g. math) and a dot (.), name of entity (pi,sin)
import numpy as np
x = np.array([3, 4, 5])
y = np.array([4, 9, 7])
print(x+y)

from math import pi, sin
print(sin(pi/2))
print(math.e) # is Euler's number: 2.71828

from math import sin, pi
print(sin(pi/2))

pi = 3.14
def sin(x):
    if 2 * x == pi:
        return 0.99999999
    else:
        return None

pi = 3.14

def sin(x):
    if 2 * x == pi:
        return 0.99999999
    else:
        return None


print(sin(pi / 2))

from math import sin, pi


from math import *

from math import pi as PI, sin as sine
  
print(sine(PI/2))
  




















